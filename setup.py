from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Internship documents, code, figures etc. for the (remote) internship at INRIA.',
    author='Robert van de Vlasakker',
    license='MIT',
)
